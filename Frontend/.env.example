# URL to backend API (used by frontend to call backend endpoints)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Optional: public base URL for uploads (if different)
NEXT_PUBLIC_UPLOADS_URL=/uploads

# If you use an env var to enable debug logging
NEXT_PUBLIC_DEBUG=true
